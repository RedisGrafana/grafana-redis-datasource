apiVersion: 1

datasources:
  - name: Redis
    type: grafana-redis-datasource
    access: proxy
    orgId: 1
    isDefault: true
    version: 1
    url: redis://localhost:6379
    jsonData:
      poolSize: 5
      timeout: 10
      pingInterval: 0
      pipelineWindow: 0
    editable: true
